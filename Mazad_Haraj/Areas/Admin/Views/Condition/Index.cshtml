@* @model List<Mazad.Models.Condition>
<div class="page-body">

    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="page-header">
            <div class="row">
                <div class="col-lg-6">
                    <div class="page-header-left">
                        <h3>
                            قائمة الشروط والاحكام
                            <small>مزاد حراج للسيارات</small>
                        </h3>
                    </div>
                </div>
                <div class="col-lg-6">
                    <ol class="breadcrumb pull-right">
                        <li class="breadcrumb-item"><a asp-controller="Home" asp-action="Index"><i data-feather="home"></i></a></li>
                        <li class="breadcrumb-item">الاعدادات</li>
                        <li class="breadcrumb-item active">الشروط والاحكام</li>
                    </ol>
                </div>
            </div>
        </div>
    </div>
    <!-- Container-fluid Ends-->
    <!-- Container-fluid starts-->
    <div class="container-fluid">
        <div class="card">
            <div class="card-header">

                <div class="btn-popup pull-right" style="float: right;">
                    <a asp-controller="Condition" asp-action="Create" class="btn btn-secondary">انشاء الشروط والاحكام</a>
                </div>
            </div>

            <div id="batchDelete" class="category-table user-list order-table jsgrid">
                <div class="jsgrid-grid-header jsgrid-header-scrollbar">
                    <div class="container mt-5">
                        <h3>جدول الشروط والاحكام</h3>
                        @if(Model.Count == 0){
                            <h2>لا يوجد شروط واحكام قم بالانشاء</h2>
                        }
                        else
                        {
                            <table class="table table-bordered table-striped table-hover">
                                <thead>
                                    <tr class="table-primary">
                                        <th class="description-column col-3">الوصف</th>
                                        <th class="title-column col-5">العنوان</th>
                                        <th class="actions-column col-3">العمليات</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    @foreach (var con in Model)
                                    {
                                        <tr>
                                            <td>@con.KVal</td>
                                            <td>@con.Title</td>
                                            <td>
                                                <a asp-controller="Condition" asp-action="Edit" asp-route-Key="@con.KKey" class="btn btn-danger">تعديل</a>
                                                <a asp-controller="Condition" asp-action="Delete" asp-route-Key="@con.KKey" class="btn btn-primary">حذف</a>
                                            </td>
                                        </tr>
                                    }



                                    <!-- يمكنك إضافة المزيد من الصفوف هنا -->
                                </tbody>
                            </table>

                        }
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
 
<!-- Container-fluid Ends-->
 *@